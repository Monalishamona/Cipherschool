#include <iostream>
using namespace std;
int main(){
   //each element require memory
   //pointer - are also elements  which can store the address
   //it store the address rather than the value
   //uses of pointer: it return multipe value, reduces length and complexity of a program
   
   int a;
   int *p;
   p = &a;
}